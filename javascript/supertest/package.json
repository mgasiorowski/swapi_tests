{
  "name": "swapi-integration-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --colors 'test/*.spec.js'",
    "test-with-docker": "docker build -t swapi-tests-supertest . && docker run --rm swapi-tests-supertest"
  },
  "devDependencies": {
    "mocha": "^4.0.1",
    "supertest": "^3.0.0"
  }
}
